<?xml version="1.0" encoding="utf-8" ?>
<Window x:Class="PersianDownloadManager.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:dev="using:DevWinUI"
        xmlns:local="using:PersianDownloadManager.Views"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">

    <Grid x:Uid="FlowDirection">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="{x:Bind dev:ProcessInfoHelper.ProductNameDisplay}" BackRequested="AppTitleBar_BackRequested" FlowDirection="LeftToRight" IsBackButtonEnabled="{x:Bind MainFrame.CanGoBack, Mode=OneWay}" IsBackButtonVisible="True" Subtitle="{x:Bind dev:ProcessInfoHelper.VersionWithPrefixDev}">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="ms-appx:///Assets/AppIcon.png" />
            </TitleBar.IconSource>
        </TitleBar>
        <Border Grid.Row="1" Margin="40,20,0,0" Padding="10" Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8,0,0,8" Opacity="{x:Bind BreadCrumbNav.Opacity, Mode=OneWay}">
            <dev:BreadcrumbNavigator x:Name="BreadCrumbNav" />
        </Border>

        <Frame x:Name="MainFrame" Grid.Row="2" />
    </Grid>
</Window>
